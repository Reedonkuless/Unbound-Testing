{
  "buildCommand": "node deploy/verify-package.mjs && rm -rf dist && mkdir -p dist && unzip -q deploy/Unbound_Save_Studio_v0.5.1_Recovered_CP33_CleanBase.zip -d dist && node deploy/patch-index.mjs && node deploy/patch-ios-rom-patcher.mjs",
  "outputDirectory": "dist",
  "cleanUrls": false,
  "trailingSlash": false,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {"key": "X-Content-Type-Options", "value": "nosniff"},
        {"key": "Referrer-Policy", "value": "no-referrer"}
      ]
    }
  ]
}
